name: Parse Design Tokens

on:
  pull_request:
    branches:
      - master

jobs:
  job1:
    runs-on: ubuntu-latest

    steps:
    - name: install dependecies
      npm install

    - name: run style dictionary
      run: npm run buildTokens
